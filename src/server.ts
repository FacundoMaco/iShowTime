// Archivo placeholder para servidor Express. SSR deshabilitado en este proyecto.
// Si necesitas SSR, actualiza Angular y ajusta este archivo.

import express from 'express';

const app = express();

// Aquí podrías agregar endpoints de API o lógica de servidor en el futuro.

export default app;
